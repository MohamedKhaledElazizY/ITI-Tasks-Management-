@* @model SmartTask.Bl.Helpers.PaginatedList<SmartTask.Core.Models.ApplicationUser> *@
@* @model SmartTask.Web.ViewModels.UsersViewModel

@{
    ViewData["Title"] = "Users";
    var prevDisabled = !Model.Users.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Users.HasNextPage ? "disabled" : "";
}
<h2>Users</h2> *@
@* 


<table class="table">
    <thead>
        <tr>
            <th>Full Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-warning">Update</a>
                    <a asp-action="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this user?')">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@

@*<div>
    @if (Model.HasPreviousPage)
    {
        <a asp-action="Index" asp-route-page="@(Model.PageIndex - 1)" class="btn btn-outline-primary">Previous</a>
    }

    <span>Page @Model.PageIndex of @Model.TotalPages</span>

    @if (Model.HasNextPage)
    {
        <a asp-action="Index" asp-route-page="@(Model.PageIndex + 1)" class="btn btn-outline-primary">Next</a>
    }
</div>*@
<!-- Pagination Controls -->
@* <div class="d-flex justify-content-between">
    <div>
       
            <a asp-action="getall" asp-route-page="@(Model.PageIndex - 1)" class="btn btn-outline-primary">Previous</a>
        
    </div>

    <div>
        Page @Model.PageIndex of @Model.TotalPages
    </div>

    <div>
        @if (Model.HasNextPage)
        {
            <a asp-action="getall" asp-route-page="@(Model.PageIndex + 1)" class="btn btn-outline-primary">Next</a>
        }
    </div>
</div>
 *@

@* 
<a asp-action="Index"
   asp-route-page="@(Model.Branches.PageIndex - 1)"
   asp-route-searchString="@Model.SearchString"
   class="btn btn-primary @prevDisabled">
    Previous
</a>
<a asp-action="Index"
   asp-route-page="@(Model.Branches.PageIndex + 1)"
   asp-route-searchString="@Model.SearchString"
   class="btn btn-primary @nextDisabled">
    Next
</a> *@

@model SmartTask.Web.ViewModels.UsersViewModel

@{
    ViewData["Title"] = "Users";
    var prevDisabled = !Model.Users.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Users.HasNextPage ? "disabled" : "";
}

<h2>Users</h2>

<form asp-action="GetAll" method="get" class="mb-3">
    <input type="text" name="searchString" value="@Model.SearchString" class="form-control" placeholder="Search..." />
    <button type="submit" class="btn btn-primary mt-2">Search</button>
</form>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Full Name</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            <tr>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                <td>
                     <secure-content asp-area="" asp-controller="User" asp-action="Edit">
                    <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-warning">Update</a>
                    </secure-content>
                       <secure-content asp-area="" asp-controller="User" asp-action="Delete">
                    <a asp-action="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this user?')">Delete</a>
					</secure-content>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Pagination Controls -->
<div class="d-flex justify-content-between align-items-center">
    <a asp-action="GetAll"
       asp-route-page="@(Model.Users.PageIndex - 1)"
       asp-route-searchString="@Model.SearchString"
       class="btn btn-primary @prevDisabled">
        Previous
    </a>

    <span>Page @Model.Users.PageIndex of @Model.Users.TotalPages</span>

    <a asp-action="GetAll"
       asp-route-page="@(Model.Users.PageIndex + 1)"
       asp-route-searchString="@Model.SearchString"
       class="btn btn-primary @nextDisabled">
        Next
    </a>
</div>
